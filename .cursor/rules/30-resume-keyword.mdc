---
description: Resume session using AO-RESUME keyword
alwaysApply: true
---

# AO Resume Keyword

When the user message contains `AO-RESUME`, before proposing any new implementation:

1. **開工單一路徑（關鍵字 `AO-RESUME`＝跑正本腳本）**  
   - **Agent 必做**：在可執行終端時，於 **monorepo 根**（`git rev-parse --show-toplevel`）執行 **`powershell -ExecutionPolicy Bypass -File .\scripts\ao-resume.ps1`**。  
     **預設行為**（勿再加「第二套」完整檢查）：`fetch`；**僅 behind>0** 時 `git pull --ff-only origin main`；**`verify-build-gates`**；workflows 依賴；**`print-open-tasks`**；**`machine-environment-audit -FetchOrigin -Strict`**（見 **`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md` 2.5.1**）。與 **`align-workstation.ps1`** 同行為。  
   - **僅在 exit code 0 後**，再進行本規則第 2 步讀檔與第 3 步回覆；**exit 非 0** 時依終端輸出處置（髒樹／衝突／Strict 缺項），**不得**宣稱已與 GitHub 對齊可開工大改。  
   - **無法執行終端**：改為手動 `git fetch` + `git status -sb` + `git rev-list --left-right --count HEAD...origin/main`；**behind>0** 時請使用者 **`git pull --ff-only origin main`**（必要時 **`git pull --rebase origin main`**），並在回覆標註「未跑完整 `ao-resume.ps1`」。  
   - **30 秒自檢（補強）**：腳本成功後可再跑 **REMOTE §2.3** 三指令；判讀 **ahead behind** 時 **behind 必須為 0**，**ahead>0** 不單獨阻斷（見 §2.3）。  
   - **Autopilot 開機**：**非**本節路徑——見 **`AGENTS.md`**（`-SkipVerify -SkipStrictEnvironmentAudit -AllowUnexpectedDirty`，輕量）。  
   - **GitHub／雙機**：遠端真相 **`origin/main`**；他機 **`AO-CLOSE` push** 後，本機必須跑通 **`ao-resume.ps1`**（或手動 pull）再工作，否則進度檔過期、`push` 會被拒。

2. Read these files in order:
- `README.md`（根或 `agency-os` 視工作區而定）
- `agency-os/AGENTS.md`
- `agency-os/memory/CONVERSATION_MEMORY.md`
- `agency-os/memory/daily/` today and yesterday notes if present
- `agency-os/TASKS.md`
- `agency-os/WORKLOG.md`
- `agency-os/LAST_SYSTEM_STATUS.md` (if exists)
- `lobster-factory/docs/LOBSTER_FACTORY_MASTER_CHECKLIST.md` (if exists)
- `lobster-factory/docs/LOBSTER_FACTORY_COMPLETION_PLAN_V2.md` (if exists)

If opened folder is `agency-os` only, use paths as above; lobster paths are `../lobster-factory/docs/...`.

3. **回覆結構（`AO-RESUME` 專用；禁止「過度精簡」到省略待辦或風險）**  
   以下**五個頂層標題依序輸出**（繁中），每節都須有**可讀的實質內容**（禁止整節只剩一兩個字或「略」）：

   - **已完成** — 綜合 `memory`、`WORKLOG` 近日、`LAST_SYSTEM_STATUS`（若存在）；若本輪沒有明確完工項，**明講**並指向 `TASKS`／`WORKLOG` 供對照。

   - **目前進度** — **必含**：
     - **龍蝦工廠**：**目前 Milestone**（M1~M5；不確定時寫「待對照 `LOBSTER_FACTORY_MASTER_CHECKLIST`」）；**今日 DoD**。
     - **阻塞／風險／需注意**：**禁止**只寫單獨「無」或「沒有」二字。須綜合 **`TASKS` 開放項**、`WORKLOG`、`CONVERSATION_MEMORY`、`LAST_SYSTEM_STATUS`（若存在）、`LOBSTER_FACTORY_MASTER_CHECKLIST`／Completion Plan 開放語意、**當輪** `ao-resume.ps1`／Strict 稽核終端輸出（若代理有看到）。若有阻塞或風險：**分點**列出（含建議下一步或需使用者決策處）。若盤點後確無：**寫「本次盤點：未見需立即處理之阻塞」**，並**至少一句**說明依據（例如 Strict 結果、WORKLOG／memory 是否曾標記風險）。
     - **Git／開工裁決**：至少一句——例如 **`ao-resume.ps1` exit 0**、behind／ahead 摘要；若未跑腳本或非 0，**誠實標註**並給修復方向（見 **`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md` 2.5.1**）。

   - **未完成待辦（TASKS，`- [ ]` 全列）** — **必做**：從已讀的 **`agency-os/TASKS.md`** **逐條**列出**每一行**仍為 `- [ ]` 的待辦，**完整複製該行原文**（checkbox 與文字均保留）；並標註其上方最近的 **`## 區塊標題`**（可每條前加 `〔區塊〕`，或分區塊用清單）。**禁止**只寫「見 TASKS」「共 N 條」而不列內文——**原因**：`print-open-tasks` 僅印在終端，使用者常以聊天為準。**若**檔內**沒有任何** `- [ ]`**：寫「（0 條未完成）」**並一句話確認。列出條數應與 **`scripts/print-open-tasks.ps1`** 的 **Total open** 一致（以檔案為準；若不一致須說明原因）。

   - **其他提醒（選填）** — `integrated-status-LATEST`、當日 `memory/daily`、或 `CONVERSATION_MEMORY` 中需接續的**非 TASKS 行項目**；若無則省略本節標題。

   - **下一步** — 具體、可執行；並保留下方第 6、7 點之雙機／筆電提醒（若適用）。

4. In `已完成` 或 `目前進度` 中，若剛才 **`ao-resume.ps1` 或 Git 自檢**有執行，可一句話標註 **Git 已對齊 origin/main**，或 **提醒使用者**在 monorepo 根重跑 **`.\scripts\ao-resume.ps1`**（behind 時才會 pull）直至 exit 0。

5. Ask one focused confirmation question only if a blocker or ambiguity exists.

6. **雙機 §1.5 + audit 口頭提醒**：讀過 `TASKS.md` 後，若 **「（AO-RESUME 提醒）雙機環境對齊」** 仍為 **未勾選**，則本次回覆的 **「下一步」**（必要時 **「目前進度」**）**必含一行**，明確提醒使用者：**另一台**須依 **`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md` §1.5** 完成首次接線，並在 monorepo 根執行 `powershell -ExecutionPolicy Bypass -File .\scripts\machine-environment-audit.ps1 -FetchOrigin -Strict` 至 **PASS（無 WARN）** 後再勾該項。已勾選則無需重複。

7. **Windows 筆電／本機 WordPress（MariaDB）對齊**：若 **「雙機環境對齊」** 仍 **未勾選**，或 `memory/CONVERSATION_MEMORY.md` 已記載使用者**桌機已裝、筆電要補**，則本次回覆 **「下一步」** 除第 6 點外 **須再加一句**，要求依 **`agency-os/docs/overview/REMOTE_WORKSTATION_STARTUP.md` §1.5.1** 完成：**MariaDB.Server**、**PHP.PHP.NTS.8.4**、`scripts/setup-wp-cli-windows.ps1`、`scripts/bootstrap-local-wordpress-windows.ps1 -EnsurePhpIni`；並註明此為 **WordPress 專用 MySQL 相容層**，**非 MCP**、**不取代 Supabase**。操作正本：`lobster-factory/docs/operations/LOCAL_WORDPRESS_WINDOWS.md`；與 IDE MCP 分工對照：`agency-os/docs/operations/cursor-mcp-and-plugin-inventory.md` §4。
