# 2026-03-25 Daily Note

## 背景
- 今天目標是讓系統能長期穩定運作，並把你要的「一人跨國公司」底座（lobster-factory）用 Phase 1 先安全落地。
- 同步修復會造成健康檢查 Critical Gate FAIL 的關鍵路徑映射，確保 closeout 能通過。

## 已完成
- Critical Gate：修復 `agency-os/.cursor` 缺失相對映射（junction 指向 `D:\Work\.cursor`）
- `lobster-factory` Phase 1 工程骨架：
  - Supabase migrations：`packages/db/migrations/0001_core.sql` ~ `0006_seed_catalog.sql`
  - WP Factory manifest：`packages/manifests/wc-core.json`
  - Durable workflows（Trigger.dev 風格骨架）：
    - `packages/workflows/src/trigger/create-wp-site.ts`
    - `packages/workflows/src/trigger/apply-manifest.ts`
  - 安全與治理：
    - `scripts/validate-manifests.mjs`
    - `scripts/validate-governance-configs.mjs`
    - `scripts/bootstrap-validate.mjs`（整體基線健檢）
- 設定收工/開機可操作的 runbook 命令，補強 `agency-os/memory/CONVERSATION_MEMORY.md`
- Phase 1 驗收門檻（失敗即停）：
  - `scripts/dryrun-apply-manifest.mjs`：乾跑輸出 installPlan + `workflow_runs`/`package_install_runs` SQL template + row payload
  - `scripts/validate-dryrun-apply-manifest.mjs`：驗收 `dryrun` 輸出是否符合 wc-core + staging-only guardrail + 固定 catalog IDs（支持 `--mode=strict|fast`）
- 已確認以 `bootstrap-validate` / dryrun / 驗收腳本均可通過（避免錯誤狀態繼續擴散）
- 已重讀 `docs/spec/raw` 三份原始規格，完成「規格 vs 現況」差距盤點並回寫到 `lobster-factory/docs/LOBSTER_FACTORY_MASTER_CHECKLIST.md`
- 已完成差距第一批落地（C4-1~C4-3）：
  - `templates/woocommerce/scripts/install-from-manifest.sh`
  - `templates/woocommerce/scripts/smoke-test.sh`
  - `infra/github/workflows/validate-manifest.yml`
- 已完成差距第二批落地（C4-4~C4-5）：
  - `infra/n8n/exports/client-onboarding-flow.json`
  - `docs/ROUTING_MATRIX.md`

## 未完成
- 接上 hosting provider adapter 與 WordPress 真正 provision/shell execution（Phase 1 仍以安全模板 + guardrails 為主）
- 把 DB 真寫入流程在你指定環境中完整跑通（預設關閉，需你提供 Supabase env 才能寫）
- 規劃 Enterprise 必備工具補強路線：Sentry/PostHog/Cloudflare/Secrets/Identity

## 風險/阻塞
- DB 真寫入依賴你提供 Supabase env；若誤開寫入會有破壞風險（目前程式保留可切換開關、仍建議 staging-only）

## 下一步
1. Tomorrow：照 `CONVERSATION_MEMORY.md` 的 Runbook Commands 跑（先 strict 或 fast 擇一）
2. 乾跑通過後，再只開 `LOBSTER_ENABLE_DB_WRITES=true` 並先寫 `workflow_runs`
3. 最後才接 `package_install_runs` 狀態更新與 artifacts/log ref

## 收工檢查（AO-CLOSE）
- `powershell -ExecutionPolicy Bypass -File .\scripts\doc-sync-automation.ps1 -AutoDetect`：PASS（`reports/closeout/closeout-20260325-231338.md`）
- `powershell -ExecutionPolicy Bypass -File .\scripts\system-health-check.ps1`：PASS（Score 100%，Critical Gate PASS；`reports/health/health-20260325-231344.md`）
- `powershell -ExecutionPolicy Bypass -File .\scripts\system-guard.ps1 -Mode manual`：PASS（`reports/guard/guard-20260325-231349.md`）

