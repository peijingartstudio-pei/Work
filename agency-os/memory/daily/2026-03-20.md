# 2026-03-20 Daily Note

## 背景
- 今日目標是把環境調整到可長時間穩定運行，處理排程路徑漂移、Cursor OOM 風險與收工流程標準化。

## 已完成
- 停用架構期不必要的 adhoc 輪詢（移除 `AgencyOS-company-a-adhoc`，並新增 `adhoc_enabled: false`）
- 修復排程註冊腳本路徑引號問題（`REGISTER_SYSTEM_GUARD_TASKS.ps1`、`REGISTER_TENANT_TASKS.ps1`）
- 重建 `AgencyOS-*` 排程並確認指向 `D:\Work\agency-os`
- 強化 `system-health-check.ps1`：新增排程 `-File` / `-WorkspaceRoot` 路徑存在性檢查
- 調整 `d:\Work\soulfulexpression.code-workspace`，降低檔案監看壓力（排除 `dist/reports/.agency-state/node_modules`）
- 修正 `RESUME_AFTER_REBOOT.md` 路徑為 `D:\Work\agency-os`，續接訊息收斂為 `AO-RESUME`
- 新增 `scripts/archive-old-reports.ps1` 與 README 入口，建立 reports 歸檔機制（預設 preview）
- 完成 closeout 三步驟：doc-sync / health-check / system-guard 全數 PASS

## 未完成
- 以新客戶完整實跑 `tenants/NEW_TENANT_ONBOARDING_SOP.md`
- 建立跨國稅務與法遵顧問審核流程
- `company-a` 真實資料填寫與第一案流程實跑

## 風險/阻塞
- Cursor OOM 風險已下降，但仍需觀察一段時間是否再出現 renderer crash
- 排程由舊路徑修復為新路徑後，仍建議明日查看一次 task history 確認穩定

## 下一步
- 明日先用 `AO-RESUME` 續接，確認 `LAST_SYSTEM_STATUS.md` 為 PASS
- 先選 1 個真實 onboarding case 執行並回寫 `WORKLOG.md`
- 每週固定跑一次 `archive-old-reports.ps1 -KeepDays 30 -Apply`
