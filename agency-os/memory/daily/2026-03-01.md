# 2026-03-01 Daily Memory

## 背景
- 今天針對 `agency-os` 的 Cursor 規則做整體整理，目標是讓開工/收工流程可標準化與可驗證。

## 已完成
- 盤點並確認現有 4 張規則：`00`、`10`、`20`、`30`。
- 更新 `00-session-bootstrap.mdc`，開場固定為：
  - `Yesterday Recap`
  - `Today Plan`
  - `Confirm`
- 新增 `40-shutdown-closeout.mdc`，支援 `AO-CLOSE` 收工流程。
- 更新 `20-doc-sync-closeout.mdc`，加入 `system-health-check` 與 Critical Gate PASS 要求。
- 更新 `AGENTS.md`，加入 `AO-CLOSE` 行為與輸出格式。
- 更新連動檔：
  - `docs/change-impact-map.json`
  - `docs/CHANGE_IMPACT_MATRIX.md`
- 新增 `.cursor/rules/README.md`（rules 使用說明）。
- 執行 closeout 三連檢查：
  - `doc-sync-automation -AutoDetect`
  - `system-health-check`
  - `system-guard -Mode manual`
- 今日檢查結果：PASS，health 分數 `100% (238/238)`。

## 未完成
- 尚未把 `AO-RESUME / AO-CLOSE` 超短操作卡放到 `README.md` 首頁。
- 尚未做一次「開工到收工」完整口令演練紀錄。

## 風險/阻塞
- 若未固定使用 `AO-RESUME` / `AO-CLOSE`，有機率出現流程漏跑（尤其是日結輸出）。

## 下一步
- 在 `README.md` 新增 1 分鐘上手區塊（兩個關鍵字 + 預期輸出）。
- 實測一次完整流程並把結果寫回 `WORKLOG.md` 與 memory。
