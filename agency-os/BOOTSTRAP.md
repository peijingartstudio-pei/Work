# BOOTSTRAP

第一次啟用此系統時，按順序完成以下動作：

1. 建立 `memory/daily/` 資料夾（若尚未存在）
2. 建立今日筆記檔 `memory/daily/YYYY-MM-DD.md`
3. 在 `WORKLOG.md` 新增「初始化完成」紀錄
4. 在 `TASKS.md` 建立前 3 個可執行任務
5. 確認 `.cursor/rules/` 已啟用

完成後，保留本檔作為新環境初始化清單，不需刪除。
