# 示例 shell（非 SSOT）

`install-from-manifest.sh` 若存在於此，僅作 **路徑／敘事參考**。

**實際由 executor 使用的 install／rollback 腳本** 以：

`lobster-factory/templates/woocommerce/scripts/`

為準（見 **ADR 001**、`installManifestStaging` / `resolveInstallScriptPath`）。
