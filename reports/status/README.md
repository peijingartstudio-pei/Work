Canonical integrated status lives in:

- `agency-os/reports/status/integrated-status-LATEST.md`

This root `reports/` tree is deprecated and kept only for backward compatibility.
All health, closeout, guard, and status outputs must be generated under `agency-os/reports/`.
Do not treat root `reports/` files as source of truth.
